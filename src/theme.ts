import { ThemeType } from 'grommet';

const ThemeGrommet = {
  defaultMode: 'light',
  global: {
    colors: {
      dark: '#111',
      light: '#FFF',
    },
    font: {
      family: 'Roboto',
    },
  }
} as unknown as ThemeType;

export default ThemeGrommet;